# Persais Environment Variables

# === REQUIRED ===

# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_REGISTRATION_SECRET=your_registration_secret

# Database (Neon for production, local for dev)
DATABASE_URL=postgresql://persais:persais_dev@localhost:5432/persais

# OpenRouter (AI Gateway)
OPENROUTER_API_KEY=your_openrouter_api_key

# === MODELS (all have defaults) ===

# DEFAULT_MODEL=openrouter/moonshotai/kimi-k2.5
# FALLBACK_MODEL=google/gemini-2.0-flash-001
# CLASSIFIER_MODEL=google/gemini-2.0-flash-001
# CODER_MODEL=anthropic/claude-sonnet-4
# OPENROUTER_PROVIDER_ORDER=Moonshot
# OPENROUTER_ALLOW_FALLBACKS=true
# MODEL_TIMEOUT=60000

# === OPTIONAL SERVICES ===

# Public URL for webhooks (Telegram, Scheduler, etc.)
# APP_BASE_URL=https://your-app.fly.dev

# Server
PORT=3000
NODE_ENV=development

# Deepgram (Voice-to-text, optional)
# DEEPGRAM_API_KEY=your_deepgram_api_key
# DEEPGRAM_LANGUAGE=ru

# Git (for Mechanic agent, optional)
# GIT_REMOTE_URL=https://github.com/yourusername/persais.git
# GIT_ACCESS_TOKEN=your_github_token

# Web Access — search + extract (optional, enables web_search/web_extract tools)
# Uses Tavily. Each key ≈ 1000 free credits/month. Multiple keys = more credits.
# WEB_ACCESS_API_KEYS=tvly-key1,tvly-key2
# WEB_ACCESS_MONTHLY_LIMIT=995

# Scheduler — Cronos integration (optional, enables manage_schedule tool)
# CRONOS_API_KEY=your_cronos_api_key
# SCHEDULER_WEBHOOK_SECRET=your_webhook_secret
